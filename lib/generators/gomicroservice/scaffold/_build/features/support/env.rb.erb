require 'cucumber-api'
require 'minke'

discovery = Minke::Docker::ServiceDiscovery.new ENV['DOCKER_PROJECT'], Minke::Docker::DockerRunner.new
$SERVER_PATH = "http://#{discovery.bridge_address_for ENV['DOCKER_NETWORK'], '<%= application_name %>', '8001'}"

Before do |scenario|

end

After do |scenario|

end
